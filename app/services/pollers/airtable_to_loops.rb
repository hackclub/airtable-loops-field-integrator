module Pollers
  class AirtableToLoops
    def call(sync_source)
      # TODO: put the per-airtable base logic here
      puts "put the per-airtable base logic here (source_id=#{sync_source.source_id})"
    end
  end
end


