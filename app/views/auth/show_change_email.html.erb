<% content_for :title, "Change Email" %>

<div class="container-narrow">
  <h1>Change Email</h1>

  <% if flash[:notice] %>
    <div class="alert alert-notice"><%= flash[:notice] %></div>
  <% end %>

  <% if flash[:error] %>
    <div class="alert alert-error"><%= flash[:error] %></div>
  <% end %>

  <% if @current_email %>
    <div class="info-box" style="margin-bottom: 16px;">
      You're currently logged in as <strong><%= @current_email %></strong>
    </div>
  <% end %>

  <%= form_with url: auth_change_email_request_path, method: :post, local: true do |form| %>
    <div class="form-group">
      <%= form.label :email, "New Email Address" %>
      <%= form.email_field :email, required: true, autofocus: true, placeholder: "new@email.com" %>
    </div>

    <div class="form-actions">
      <%= form.submit "Send OTP Code", class: "btn btn-full" %>
    </div>
  <% end %>

  <div style="margin-top: 16px; text-align: center;">
    <%= link_to "← Cancel", profile_edit_path, class: "btn btn-secondary btn-full" %>
  </div>

  <p class="text-muted" style="margin-top: 32px; font-size: 14px;">
    We'll send a one-time password (OTP) to your new email address to verify your identity. You'll remain logged in until you verify the new email.
  </p>
</div>

